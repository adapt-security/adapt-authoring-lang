{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "type": "object",
  "properties": {
    "locale": {
      "type": "string",
      "description": "The language to be used by the back-end server for log messages etc.",
      "default": "en"
    },
    "supportedLanguages": {
      "type": "array",
      "description": "The languages which are supported by the back-end server",
      "items": { "type": "string" },
      "default": ["en"]
    }
  }
}
